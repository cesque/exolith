import data from '@/data/tarotDecks.js'
import TarotDeck from '@/components/TarotDeck/TarotDeck'

export const meta = {
    title: 'Tarot Decks',
    tags: ['personal', 'occult'],
}

[View as grid](/tarot-grid)

{ data.map(deck => {
    return <TarotDeck deck={ deck } />
})}